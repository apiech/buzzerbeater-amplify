{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "exclude": [
    "node_modules",
    "amplify",
    "tests",
    ".amplify",
    ".next",
    "cdk.out",
    "out",
    "build"
  ]
}
